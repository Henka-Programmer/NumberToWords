using System;
using System.Collections.Generic;
using System.Text;

namespace NumberToWords {
  public enum LetterCase {
    Lowercase = 0,
    Uppercase = 1,
    TitleCase = 2,
    SentenceCase = 3
  }
}
